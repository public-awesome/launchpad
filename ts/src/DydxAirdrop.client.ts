/**
* This file was automatically generated by @cosmwasm/ts-codegen@0.35.3.
* DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
* and run the @cosmwasm/ts-codegen generate command to regenerate this file.
*/

import { CosmWasmClient, SigningCosmWasmClient, ExecuteResult } from "@cosmjs/cosmwasm-stargate";
import { Coin, StdFee } from "@cosmjs/amino";
import { InstantiateMsg, Member, ExecuteMsg, QueryMsg, Boolean, Addr } from "./DydxAirdrop.types";
export interface DydxAirdropReadOnlyInterface {
  contractAddress: string;
  airdropEligible: ({
    ethAddress
  }: {
    ethAddress: string;
  }) => Promise<Boolean>;
  getMinter: () => Promise<Addr>;
}
export class DydxAirdropQueryClient implements DydxAirdropReadOnlyInterface {
  client: CosmWasmClient;
  contractAddress: string;

  constructor(client: CosmWasmClient, contractAddress: string) {
    this.client = client;
    this.contractAddress = contractAddress;
    this.airdropEligible = this.airdropEligible.bind(this);
    this.getMinter = this.getMinter.bind(this);
  }

  airdropEligible = async ({
    ethAddress
  }: {
    ethAddress: string;
  }): Promise<Boolean> => {
    return this.client.queryContractSmart(this.contractAddress, {
      airdrop_eligible: {
        eth_address: ethAddress
      }
    });
  };
  getMinter = async (): Promise<Addr> => {
    return this.client.queryContractSmart(this.contractAddress, {
      get_minter: {}
    });
  };
}
export interface DydxAirdropInterface extends DydxAirdropReadOnlyInterface {
  contractAddress: string;
  sender: string;
  register: ({
    ethAddress,
    ethSig
  }: {
    ethAddress: string;
    ethSig: string;
  }, fee?: number | StdFee | "auto", memo?: string, _funds?: Coin[]) => Promise<ExecuteResult>;
  claimAirdrop: ({
    ethAddress,
    ethSig
  }: {
    ethAddress: string;
    ethSig: string;
  }, fee?: number | StdFee | "auto", memo?: string, _funds?: Coin[]) => Promise<ExecuteResult>;
}
export class DydxAirdropClient extends DydxAirdropQueryClient implements DydxAirdropInterface {
  client: SigningCosmWasmClient;
  sender: string;
  contractAddress: string;

  constructor(client: SigningCosmWasmClient, sender: string, contractAddress: string) {
    super(client, contractAddress);
    this.client = client;
    this.sender = sender;
    this.contractAddress = contractAddress;
    this.register = this.register.bind(this);
    this.claimAirdrop = this.claimAirdrop.bind(this);
  }

  register = async ({
    ethAddress,
    ethSig
  }: {
    ethAddress: string;
    ethSig: string;
  }, fee: number | StdFee | "auto" = "auto", memo?: string, _funds?: Coin[]): Promise<ExecuteResult> => {
    return await this.client.execute(this.sender, this.contractAddress, {
      register: {
        eth_address: ethAddress,
        eth_sig: ethSig
      }
    }, fee, memo, _funds);
  };
  claimAirdrop = async ({
    ethAddress,
    ethSig
  }: {
    ethAddress: string;
    ethSig: string;
  }, fee: number | StdFee | "auto" = "auto", memo?: string, _funds?: Coin[]): Promise<ExecuteResult> => {
    return await this.client.execute(this.sender, this.contractAddress, {
      claim_airdrop: {
        eth_address: ethAddress,
        eth_sig: ethSig
      }
    }, fee, memo, _funds);
  };
}